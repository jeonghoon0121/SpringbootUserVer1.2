<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.org.userproject.UserMapper">

    <!-- 결과 매핑 정의 -->
    <resultMap id="userResultMap" type="com.org.userproject.UserDTO">
        <id property="userId" column="user_id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="email" column="email"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <!-- 회원 조회 (username 기준) -->
    <select id="findByUsername" parameterType="String" resultMap="userResultMap">
        SELECT user_id, username, password, email, created_at
        FROM users
        WHERE username = #{username}
    </select>
    <insert id="insertUser" parameterType="com.org.userproject.UserDTO">
        INSERT INTO users (username, password, email, created_at)
        VALUES (#{username}, #{password}, #{email}, NOW())
    </insert>

</mapper>
